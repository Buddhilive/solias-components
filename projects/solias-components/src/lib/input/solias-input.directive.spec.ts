import { SoliasInputDirective } from './solias-input.directive';

describe('SoliasInputDirective', () => {
  it('should create an instance', () => {
    const directive = new SoliasInputDirective();
    expect(directive).toBeTruthy();
  });
});
