import { SoliasButtonDirective } from './solias-button.directive';

describe('SoliasButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SoliasButtonDirective();
    expect(directive).toBeTruthy();
  });
});
